# 시스템 해부

> 이 문서는 **왜 그렇게 만들었는가**를 적습니다. 무엇을 고칠지는
> [TROUBLESHOOTING.md](TROUBLESHOOTING.md), 실물 구조 측정치는
> [_reference/SOURCE_NOTES.md](_reference/SOURCE_NOTES.md)에 있습니다.

---

## 0. 설계 원칙 네 가지

**① 규칙은 코드가 아니라 설정에 둔다.**
컬럼 이름·날짜 우선순위·Category 변환·리드타임·색상이 전부 `config.json`에
있습니다. 업무 규칙은 바뀌지만 코드를 고치게 하면 사람이 겁을 냅니다.

**② 색은 만들어내지 않는다. 양식에서 배운다.**
바 색상에는 팀의 규칙이 담겨 있습니다. 임의로 정하면 안 됩니다. 양식에 이미
칠해진 색을 Category별로 세어 가장 많이 쓰인 것을 그 Category의 색으로 씁니다.

**③ 추측은 실측할 길과 함께 둔다.**
모르는 규칙은 아는 척하지 않고 `config.json`에 가정으로 적고, `calibrate`로
실물에서 역산할 수 있게 했습니다.

**④ 전량 재생성.**
일부만 갱신하지 않고 매번 통째로 다시 그립니다. 그래야 "동기화 깜빡함"이
원천적으로 불가능해집니다.

---

## 1. 파이프라인

```
                 ┌─────────────────────────────────────────┐
 RA plan.xlsx ──▶│ ① 판독   detect_source_columns          │
                 │    헤더 행 자동탐지, PRA/NDA 그룹 분리   │
                 └────────────────┬────────────────────────┘
                                  ▼
                 ┌─────────────────────────────────────────┐
                 │ ② 정규화  build_records                  │
                 │    날짜 판독 → Sub/App 결정              │
                 │    Category 변환, 준비기간 계산          │
                 └────────────────┬────────────────────────┘
                                  ▼
                 ┌─────────────────────────────────────────┐
 양식.xlsx    ──▶│ ③ 채우기  write_into_template            │
                 │    양식 복사 → 값만 기입 → 바 칠하기     │
                 │    색은 learn_palette / config_palette   │
                 └────────────────┬────────────────────────┘
                                  ▼
 이전 간트     ─▶ ④ 비교  diff_records  ──▶  결과.xlsx + 변경내역
```

---

## 2. ① 판독 — 왜 자동탐지인가

컬럼 위치를 상수로 박으면 파일이 한 번만 바뀌어도 죽습니다. 실물만 봐도
헤더가 **4행**에 있고, 위에 안내 문구 행과 그룹 제목 행이 끼어 있습니다.

**헤더 행 찾기** — 상단 25행 중 별칭이 가장 많이 맞는 행을 헤더로 봅니다.

**별칭은 완전일치가 아니라 포함 매칭**입니다. 실물 헤더가
`Responsible\n20260608~`처럼 개정일자를 달고 있기 때문입니다. 완전일치로 하면
못 찾습니다.

**Responsible이 둘일 때** — A열(구 담당자)과 B열(현 담당자)이 나란히 있습니다.
`responsible_prefer_marker`(기본 `"20260608"`)가 든 쪽을 쓰고, 표식이 없으면
가장 오른쪽을 씁니다. 실측 결과 간트는 B열과 5/5 일치했습니다.

**PRA/NDA 그룹 분리 — 가장 중요한 부분**

날짜 컬럼 8개는 이름만으로 구분되지 않습니다. 양쪽 다 `Planned Submission`입니다.
그래서 헤더 **위쪽의 병합된 그룹 제목**을 찾아 그 병합 범위(`min_col~max_col`)를
경계로 삼습니다.

```
        K3:N3 = "Pre-review (사전검토 별도 하는 경우)"   → K,L,M,N = PRA
        O3:R3 = "NDA or Variation (One step)"           → O,P,Q,R = NDA
```

제목을 못 찾으면 "앞 4개=PRA, 뒤 4개=NDA"로 넘어가되 **경고를 남깁니다.**
조용히 틀리는 것보다 시끄럽게 맞는 편이 낫기 때문입니다.

> ⚠️ 초기에는 이 구조를 몰라서 "여덟 날짜 중 하나를 고르는 우선순위 규칙"이라는
> **존재하지 않는 문제**를 풀고 있었습니다. 스크린샷의 오른쪽이 잘려 있어
> Sub/App이 한 쌍으로 보였기 때문입니다.

---

## 3. ② 정규화

### 날짜 판독 — 현실은 지저분하다

실물 날짜 칸의 값 분포(실측): `N/A` 1508건, `TBD` 858건, `TBU` 39건,
`2026-12` 22건, `2028-03` 18건, `2026-12-31\n(TBD)` 8건, `Not required` 8건,
`2022-9(E)` 4건, `2025 Oct` 2건, `2024-11-30\n2024-12-13` 2건 …

`parse_cell_date()`가 처리하는 순서:

1. 진짜 날짜 객체면 그대로
2. `unresolved_tokens`에 있으면 미정
3. 꼬리의 괄호 주석 제거 (`(TBD)`, `(E)`, `(W)`)
4. 날짜가 둘 적혀 있으면 첫 번째만
5. `YYYY-MM-DD` → 확정 날짜
6. `YYYY nQ` / `YYYY Mon` / `YYYY-MM` / `YYYY` → **`partial=True`**로 표시

`partial`이 핵심입니다. RA plan 2행의 형식 안내가 `TBD, YYYY nQ, YYYY-MM-DD`
이므로 **일(日)이 없는 표기는 "아직 확정 안 됨"의 공식 표기**입니다.
날짜처럼 생겼지만 날짜로 취급하면 안 됩니다.

### Sub/App 결정

각 쌍(PRA, NDA) 안에서 독립적으로 정합니다. 쌍 사이를 넘나들지 않습니다 —
그게 원본 구조이기 때문입니다.

```
Actual이 있으면 Actual        (실제로 일어난 일이 사실이므로)
없으면 Planned
단, reject_partial_dates=true면 partial 값은 건너뛰고 다음 후보로
전부 partial뿐이면 그거라도 쓴다 (빈칸보다는 낫다)
```

> **검증**: 기준행(충전라인 신설) — PreRev Plan App `2026-08-31`, PreRev Act App
> `2026-05-06`, NDA Plan App `2027/01`, NDA Act App `TBD`, 상태 Ongoing.
> 실제 간트의 값은 `2026-08-31`. `2027/01`을 partial로 건너뛰어야 재현됩니다.
> 이 케이스는 `tests/test_rules.py`에 못 박아 두었습니다.

### Category 변환

RA plan에는 20종이 있지만 간트에는 7종만 나타납니다.

```
category_map      명시적 대응 (예: "NDA (Must-Win)" → "NDA (variant)")
category_passthrough  그대로 두는 목록
그 외              전부 category_fallback ("Others")
```

**리드타임은 변환 후가 아니라 원본 Category 기준**입니다. `CMC`는 간트에
`Others`로 표시되지만 준비기간은 `CMC`의 4개월을 씁니다. 표시용 이름과
업무적 성격은 다르기 때문입니다.

### 준비기간

```
prep_start = sub - lead_months[원본 Category]
```

`add_months()`는 말일 넘침을 처리합니다 (3/31 -1개월 = 2/28, 윤년이면 2/29).

> ⚠️ `lead_months_by_category`는 **실측값이 아닙니다.** 규제 관행 기준 초안이며
> 이 문서에서 유일하게 근거가 약한 부분입니다. 실제 간트의 연한 바 시작점을
> 재서 교체해야 합니다.

---

## 4. ③ 채우기 — 양식이 곧 법

### 원본을 건드리지 않는다

`shutil.copyfile`로 복사한 뒤 사본에만 씁니다.

### 서식 유지

기존 데이터 행의 스타일(글꼴·테두리·정렬·표시형식)을 견본으로 캐시해 두고,
행이 늘어나면 그 스타일을 복제합니다. 열너비·병합·틀고정·조건부서식은
애초에 건드리지 않으므로 그대로 남습니다.

### 개정 스냅샷 탭

결과 시트 이름을 `20260802`처럼 날짜로 바꿉니다. 기존의 "개정할 때마다 탭
복제" 관행을 그대로 이어받은 것입니다 — 사람이 하던 일을 없애는 게 아니라
**자동으로 해주는** 쪽을 골랐습니다.

### 색 — 두 단계

```python
palette = config_palette(config)        # 실물에서 추출해 구워둔 값
palette.update(learn_palette(ws, ...))  # 양식에 바가 있으면 그쪽이 이긴다
```

`learn_palette()`는 양식의 각 행에서 제출일 이전 구간과 제출~승인 구간의
채우기 색을 세어, Category별 최빈값을 그 Category의 색으로 삼습니다.

**실측 결과 — 색조는 상태가 아니라 Category가 정합니다:**

| Category | 색조 | 일치율 | 표본 |
|---|---|---|---|
| Others | theme9 | 90% | 759 |
| NDA | theme7 | 95% | 238 |
| NDA (variant) | theme7 | 74% | 116 |
| Site addition | theme6 | 100% | 222 |
| New indication | theme6 | 100% | 130 |
| CMC Must-Win | theme6 | 91% | 87 |

Product별·Responsible별로는 주색 비중이 40~50%대로 상관이 없었습니다.
진하기(tint)는 준비 0.8(연함) / 검토 0.4~0.6(진함)으로 갈립니다.

### 바 그리기

PRA와 NDA 두 구간을 각각 그립니다. 겹치면 **검토(진한색)가 준비(연한색)를
덮습니다** — 진행된 사실이 계획보다 우선이기 때문입니다.
양식의 월 범위 밖은 그리지 않습니다(실물도 그리드 시작에서 잘려 있습니다).

---

## 5. ④ 비교 — 이 도구의 존재 이유

매니저가 알고 싶은 것은 "간트 파일"이 아니라 **"지난번 이후 무엇이 바뀌었나"**입니다.

```
added          팀원이 RA plan에 새로 만든 프로젝트
moved          제출일/승인일이 바뀐 건 (가장 중요)
status_changed 상태만 바뀐 건
removed        이전엔 있었으나 지금 RA plan에 없는 건
```

**행의 신원은 `제품명 + 과제명`입니다.** 행 번호가 아닙니다 — 팀원이 중간에
새 프로젝트를 끼워 넣으면 아래 행이 전부 밀리기 때문입니다. 이 선택 때문에
과제명을 고치면 "삭제 + 신규"로 잡히는데, 그건 감수할 만한 대가로 봤습니다.

---

## 6. 미리보기가 실제 색을 보여주는 법

엑셀은 색을 테마 인덱스 + tint로 저장합니다(`theme9 + tint0.6`). 화면에
그대로 보여주려면 RGB로 풀어야 합니다.

1. `read_theme_colors()` — 통합문서의 `theme1.xml`에서 테마 색 12개를 읽습니다.
   못 읽으면 Office 기본 테마로 근사합니다.
2. `apply_tint()` — 엑셀 규칙 그대로 HSL 명도에 적용합니다.
   `tint>0`이면 `L' = L(1-t) + t`, `tint<0`이면 `L' = L(1+t)`.

덕분에 **파일을 열지 않고도 결과를 확인**할 수 있습니다.
엑셀 파일 자체의 색은 언제나 정확하고, 화면만 근사값일 수 있습니다.

---

## 7. calibrate — 추측을 실측으로

```bash
python gantt_engine.py calibrate <RA_plan.xlsx> <사람이_만든_간트.xlsx>
```

간트의 각 날짜 컬럼 값이 RA plan의 어느 컬럼 값 집합에 속하는지를 세어
일치율로 보여줍니다. **행 단위로 짝짓지 않아도 동작하는 것이 요점**입니다 —
두 파일이 서로 다른 시점의 스냅샷이거나 과제명이 지워진 익명화 사본이어도
값 대조는 유효합니다.

이 설계는 실제로 필요했습니다. 받은 RA plan 사본은 Project 컬럼이 통째로
비워져 있어 과제명 매칭이 불가능했습니다.

---

## 8. 이 프로젝트에서 틀렸던 것들

기록해 둡니다. 같은 실수를 반복하지 않기 위해서입니다.

| 무엇을 | 왜 틀렸나 | 어떻게 알았나 |
|---|---|---|
| Sub/App을 한 쌍으로 봄 | 스크린샷 오른쪽이 잘려 있었음 | 실물 파일의 `E2='PRA'`, `G2='NDA or variations'` |
| 색을 상태별로 봄 | 사진으로는 테마 색을 분간할 수 없음 | 상태별 집계에서 theme6/7/9이 모두 섞여 나옴 |
| App을 NDA 우선으로 고름 | 근거 없이 "본제출이 최종"이라 가정 | 겹치는 한 행이 반증 |
| `2027/01`을 날짜로 취급 | 날짜처럼 생겼음 | 그렇게 하면 실제 간트값이 재현 안 됨 |

**교훈**: 서식·색상이 규칙인 파일은 스크린샷으로 판독하면 안 됩니다.
실물을 받아 `openpyxl`로 직접 읽어야 합니다.
